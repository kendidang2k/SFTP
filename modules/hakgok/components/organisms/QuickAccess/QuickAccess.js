const { useState, createContext, useEffect } = wp.element;
const {
    Box,
    Grid,
    Typography,
    createMuiTheme
} = MaterialUI;



function QuickAccess() {
    return (
        <Grid className="quick__access" sx={{ display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'pointer' }}>

        </Grid >
    )
}
